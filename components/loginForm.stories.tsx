import { Meta } from "@storybook/react"
import LoginForm from "./loginForm"

export default {
  title: "LoginForm",
  component: LoginForm,
  tags: ["autodocs"],
} as Meta

export const Default = {
  args: {},
}
